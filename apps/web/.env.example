# Local development
DATABASE_URL=postgresql://USER:PASSWORD@ep-xxx-pooler.region.aws.neon.tech/hl_sales?sslmode=verify-full
AUTH_SECRET=generate-random-32-character-secret
AUTH_TRUST_HOST=true
ADMIN_EMAIL=admin@hl.local
ADMIN_PASSWORD=admin123

# Vercel: set each variable twice with different scopes in Project Settings → Environment Variables.
# Production → Neon main branch connection string only.
# Preview     → Neon preview/staging branch connection string only (never share production DATABASE_URL).
